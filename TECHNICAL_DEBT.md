# Technical Debt Register

Real, verified debt only — no speculative items. Each entry was confirmed against actual code/docs/config in this repository, not assumed. Superseded by nothing; this is the living register — update it in place rather than creating a new one per session.

| # | Item | Impact | Priority | Effort | Recommended timing |
|---|---|---|---|---|---|
| 1 | Several docs describe the cron pipeline as "not yet live" / "2-hour cadence" (`design/limitations.md` §1.3, `docs/deployment.md` §11, `docs/agent-workflow.md`'s go-live gate) but `scrape.yml`'s `schedule:` is **actually active** at a 6-hour cadence, not commented out. Documentation and reality have diverged on a production-relevant fact. `design/tech-stack.md` §7 had the same stale "commented out" claim — fixed as part of the merge-workspace Phase 0 debt pass (now states the schedule is active and points here for the open question). `design/limitations.md`/`docs/deployment.md`/`docs/agent-workflow.md` remain unfixed. | Medium — anyone trusting the docs would misjudge freshness expectations and think a manual approval step is still pending when it already happened | P1 | Low (confirm intent with whoever enabled it, then update `design/limitations.md`/`docs/deployment.md` to match reality — or revert the schedule if it was accidental) | Next session — needs a human decision (was 6h deliberate?), not just a doc fix |
| 2 | `docs/database.md`'s inline schema listing predates ~10 migrations (fingerprint, canonical_company_name, contact_email*, salary_*, job-attribute columns, etc.) | Low now (banner added this session pointing to `design/erd.md`), but will keep drifting if anyone edits the inline schema instead of `erd.md` | P3 | Low (prune the stale SQL block, keep §7/§8 procedural content) | Whenever someone is already editing that file for another reason |
| 3 | `docs/repositories.md` §7b (`MatchedJobsRepository`) doesn't reflect the Phase 4 analytics additions (`getJobsCompanyData`, `getJobsSalaryData`, `getScrapeRunStats`) or the AD-19 RPC-based `ScoreRepository.insertScore` change | Low — doc is a deep-dive reference, not load-bearing for correctness | P3 | Medium (the file uses numbered sections referenced elsewhere; renumbering carelessly could break cross-references) | Bundle with a future repositories.md-touching change, not standalone |
| 4 | `docs/frontend.md` doesn't mention Phase 4's `AnalyticsCharts`/`ScrapeRunHealthTable`/`ScoringQueueStatsCards` components | Low — cosmetic staleness in a reference doc | P3 | Low | Opportunistic |
| 5 | `scripts/source-analytics.ts` and `scripts/filter-analysis.ts` query the Supabase client directly instead of going through `SupabaseScrapeRunRepository` (unlike `report-sources.ts`, which does) | Low — these are read-only CLI diagnostics, not app runtime, so the Repository Pattern's testability rationale doesn't fully apply, but it's an inconsistent precedent for future scripts | P3 | Medium (would need a repository method for the 30-day-aggregate query shape) | Only if a third script needs the same query shape — don't refactor working scripts speculatively |
| 6 | `JobMatch` test fixtures (`makeMatch()`) are reimplemented with different baseline values across 9 files in `src/features/notifications/application/*.test.ts` | Low — each touch-all-files pass (like v1.2's) has to update every copy | P3 | Medium (a shared factory would need to be parameterized enough to preserve each file's distinct test narrative) | Only if a 10th `JobMatch` field is added and this becomes actively painful |
| 7 | No Telegram alert when a source newly fails validation, despite the detection primitive (`previousHealthStatus`) already existing in `validateSources.ts`/tests | Medium — silent data-quality degradation between the weekly `validate-sources` run and whenever someone happens to check `/analytics` | P2 | Low (wire existing `TelegramBotSender` to the already-computed new-failure signal) | Good first pick for a future session — small, real, useful |
| 8 | No `scrape_runs.raw_count` (pre-role-filter count) — can't distinguish "no jobs matched the active role" from "board returned nothing at all" | Low-Medium — mostly a diagnostics/observability gap, not a correctness issue | P3 | Low (forward-only migration + one field in `scrape.ts`) | With the next scrape.ts-touching change |
| 9 | No dead-letter/backoff for a permanently-failing Telegram send (bot blocked, chat deleted) — retries forever, indefinitely, silently (`design/limitations.md` §4.1) | Low for a single-user tool (the user would notice the silence quickly) but real | P3 | Medium (needs a failure-count-then-stop policy, a deliberate product decision not just a bug fix) | Only if it's actually been observed to happen |
| 10 | `job_scores`/cost data exists (`estimated_cost_usd`, `tokens_input/output`) and is surfaced in aggregate (`TokenStatsCards`), but there's no per-model cost/quality comparison view | Low — nice-to-have, not a gap in core functionality | P3 | Medium (needs a new aggregation function + repository method + UI panel) | See `ROADMAP.md` Backlog #6 |
| 11 | Experience parsing (`parseMinYears`) has known-low regex coverage (`design/limitations.md` §8); a backfill script now exists (`backfill:min-years`, wired v1.2) but hasn't been re-run against improved regex because the regex hasn't been improved yet | Medium — the experience filter is a stated user-facing feature with real coverage gaps | P2 | Medium (regex expansion needs a validation pass against a real job-description sample to avoid false positives) | Good candidate for the next "improve deterministic extraction" pass |
| 12 | `supabase/database.types.ts` is manually hand-edited to track each migration in this sandboxed environment (no live Supabase project to run `supabase gen types` against) | Medium until a real project exists — manual edits can silently drift from the real generated output | P2 | Low (one `supabase gen types` run + diff, whenever a live project is available) | First thing to do once this project has a real, linked Supabase project |
| 13 | `design/limitations.md` §8's original technical-debt table items not yet superseded: no AI score invalidation on resume change; sequential (non-batched) AI scoring; in-memory analytics aggregation at scale | Low today (no evidence of a real performance problem at current job volume) | P3 | N/A — don't fix without evidence | Revisit only if job volume or resume-change frequency grows enough to actually hurt |
| 14 | **Scorer re-score guard lives only in the caller, not in `scoreJob`.** Investigated for the feared "re-scoring already-scored jobs = wasted LLM $" risk. Finding: NOT happening in the production path. `scoreJob.ts:52` calls the AI provider with no internal skip-guard, but the only production caller — `scripts/score.ts:61` → `SupabaseJobRepository.findUnscored` (`:342-347`) — already excludes any job whose `job_scores` row has `ai_score IS NOT NULL` for the current `(role_selection_id, resume_version)`. The exclusion is keyed on `resume_version` (`:346`), so changing the active resume (which bumps `resume.version`) already forces a full re-score — exactly the intended-re-scoring behavior. So there is **no** uncontrolled re-scoring and **no** naive "skip if scored" that would break resume-change re-scoring. Residual gap: the guard is in `findUnscored` only, so any *future* second caller of `scoreJob` outside that path would re-hit the AI provider and insert a duplicate score row. | Low — production path is correct and cost-safe today; only a latent footgun for a hypothetical future caller | P3 | Low | **Decision for the human:** leave as-is (findUnscored is the sole caller), or add a defensive guard inside `scoreJob`? **Recommendation:** leave as-is now; if a second caller ever appears, key the guard on `(job_id, role_selection_id, resume_version)` — NOT `(job_id, resume_version)` alone, since scores are per role selection too, and a resume-version-only key would wrongly skip re-scoring after a role-selection change. |
| 15 | **Telegram digest "not firing" — most-likely quiet, not broken (static audit, no logs yet).** Audited the three checks requested: **(a) env/secret names match** — `TelegramBotSender.ts:39-40` reads `TELEGRAM_BOT_TOKEN`/`TELEGRAM_CHAT_ID`; `scrape.yml:68-69` supplies both. No mismatch. **(b) no silently-swallowed send error** — a failed send throws (`TelegramBotSender.ts:58`), propagates through `sendDigestMvp.ts:64` → the top-level `.catch` in `scripts/notify.ts:84-85` → `process.exit(1)`. A real send failure turns the Action **red**, it does not exit 0. (So no `console.error` was added — the optional change was gated on finding a swallowed error, and there isn't one.) **(c) empty result → "works but sends nothing"** — `sendDigestMvp` returns early with zero counts when `findUnnotifiedMatches` yields no rows (`sendDigestMvp.ts:53`); matches are one-shot (marked notified at `:70`), so any run with no NEW ≥-threshold, unnotified matches sends no message. This is the single most-likely cause and is indistinguishable from a broken bot without logs. | Medium — the user can't tell "nothing to send" from "integration broken" without pulling logs | P2 | Low (diagnosis only) | **Decision for the human:** confirm broken-vs-quiet before changing anything. **Exact confirmation:** open the latest **Scrape pipeline** Actions run → the `npm run notify` step, and read the existing log line `[notify] digest sent — N strong match(es), M worth-reviewing` (`scripts/notify.ts:62-65`). `N=M=0` ⇒ simply nothing to send (not broken); the line **absent or accompanied by a `[notify] fatal error`** ⇒ a real failure to chase. |

## Explicitly not debt (deliberate tradeoffs, do not "fix")

These come up repeatedly in review because they look like bugs but are documented, intentional decisions — see `design/limitations.md` for the full list and `docs/decisions.md` for the rationale behind each:
- Regex-based extractors (salary/email/job-attributes) have false-negative gaps by design — a missed match is preferred over a wrong guess (AD-16, AD-20, AD-21, AD-22, AD-25).
- `notifications_log` retries indefinitely on failure with no backoff (see debt item #9 above for the one edge case worth eventually addressing — permanent failure, not transient).
- Single-user RLS model (`authenticated = full access`, no per-row ownership) — correct for this project's stated scope, not a security gap to "harden."
- Two independent, sometimes-disagreeing source-health signals shown side by side rather than merged (AD-18/AD-24) — a deliberate "fail visible" choice.
